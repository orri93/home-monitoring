 <!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements.  See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0
    (the "License"); you may not use this file except in compliance with
    the License.  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
-->
<!--

    Lets deploy some Enterprise Integration Patterns inside the ActiveMQ Message Broker
    For more information, see:

    http://camel.apache.org

    Include this file in your configuration to enable Camel

    e.g. <import resource="camel.xml"/>

-->
<beans
   xmlns="http://www.springframework.org/schema/beans"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="
     http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--
       Lets configure some Camel endpoints

       http://camel.apache.org/components.html
    -->
    <bean id="postgres-ds" class="org.postgresql.ds.PGPoolingDataSource" destroy-method="close">
        <property name="serverName" value="db" />
        <property name="databaseName" value="activemq" />
        <property name="portNumber" value="5432" />
        <property name="user" value="activemq" />
        <property name="password" value="password" />
        <property name="dataSourceName" value="postgres" />
        <property name="initialConnections" value="1" />
        <property name="maxConnections" value="10" />
    </bean>

    <bean id="activemq" class="org.apache.camel.component.jms.JmsComponent" >
        <property name="connectionFactory">
          <bean class="org.apache.activemq.ActiveMQConnectionFactory">
            <property name="brokerURL" value="vm://localhost?create=false"/>
            <property name="userName" value="${activemq.username}"/>
            <property name="password" value="${activemq.password}"/>
          </bean>
        </property>
    </bean>

    <camelContext id="camel" xmlns="http://camel.apache.org/schema/spring">

        <!-- Routes for the Indoor Ambient Monitoring Device #1 -->
        <route>
            <from uri="activemq:queue:iam.indambmon1.humidity?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon1', 'humidity', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon1.pressure?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon1', 'pressure', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon1.temperature?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon1', 'temperature', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>

        <!-- Routes for the Indoor Ambient Monitoring Device #2 -->
        <route>
            <from uri="activemq:queue:iam.indambmon2.humidity?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon2', 'humidity', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon2.pressure?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon2', 'pressure', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon2.temperature?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon2', 'temperature', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>

        <!-- Routes for the Indoor Ambient Monitoring Device #3 -->
        <route>
            <from uri="activemq:queue:iam.indambmon3.humidity?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon3', 'humidity', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon3.pressure?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon3', 'pressure', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon3.temperature?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon3', 'temperature', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>

        <!-- Routes for the Indoor Ambient Monitoring Device #4 -->
        <route>
            <from uri="activemq:queue:iam.indambmon4.humidity?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon4', 'humidity', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon4.pressure?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon4', 'pressure', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon4.temperature?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon4', 'temperature', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        
        <!-- Routes for the Indoor Ambient Monitoring Device #5 -->
        <route>
            <from uri="activemq:queue:iam.indambmon5.humidity?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon5', 'humidity', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon5.pressure?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon5', 'pressure', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>
        <route>
            <from uri="activemq:queue:iam.indambmon5.temperature?transacted=true"/>
            <unmarshal>
                <json library="Jackson"/>
            </unmarshal>
            <setBody><simple>INSERT INTO iam VALUES ('${body[ts]}', 'indambmon5', 'temperature', ${body[value]})</simple></setBody>
            <to uri="jdbc:postgres-ds"/>
        </route>

    </camelContext>

</beans>
